import React from 'react';
import '../../styles/templates/LoginTitle.css';
import LoginTitle from '../organisms/LoginTitle'

const LoginTitle = () => {
  return (
    <div className="LoginTitle">
      LoginTitle
    </div>
  );
}

export default LoginTitle;
